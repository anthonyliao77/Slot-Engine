# shaders

Add GLSL programs as you build out the renderer:
- `reel.vert` / `reel.frag` for symbol quads
- `ui.vert` / `ui.frag` for overlays and text
- post-processing passes (bloom, vignette, etc.)
