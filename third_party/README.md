# third_party

Store vendor notes and integration scripts:
- dependency managers (vcpkg, Conan) lockfiles
- license summaries for external libraries (GLFW, GLEW, ImGui)
- build/setup scripts for fetching SDKs without checking binaries into git
