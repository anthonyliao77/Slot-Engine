# assets

Store runtime art and audio resources:
- textures/sprites for reels, symbols, and UI chrome
- sound effects and background music
- fonts for dynamic text rendering

Keep source files (e.g., layered PSD/AI) in a separate `assets/source/` folder if needed.
