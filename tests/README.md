# tests

Plan statistical and regression coverage:
- RNG quality checks and distribution visualisation
- payout verification against expected return-to-player
- renderer smoke tests (headless frame dumps, snapshot comparisons)
