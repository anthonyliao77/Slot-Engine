# config

Use structured data (JSON, TOML, or YAML) to describe:
- machine denomination and wager settings
- reel strip file references and paytable IDs
- animation timing parameters and theme switches

Include sample configs such as `default.json` once the format stabilizes.
